import PropTypes from 'prop-types';

const CustomScrollBarPropTypes = {
  width: PropTypes.string,
  maxHeight: PropTypes.string,
  children: PropTypes.node.isRequired,
};

export default CustomScrollBarPropTypes;
