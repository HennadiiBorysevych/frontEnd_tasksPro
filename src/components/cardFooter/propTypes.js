import PropTypes from 'prop-types';

const CardFooterPropTypes = {
  deadline: PropTypes.string,
  id: PropTypes.string.isRequired,
  priority: PropTypes.string,
};

export default CardFooterPropTypes;
