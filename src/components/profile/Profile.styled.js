import styled from '@emotion/styled';

export const Container = styled.div({
  display: 'flex',
  alignItems: 'center',
  gap: '8px',
  cursor: 'pointer',
});
export const UserName = styled.h3({
  margin: 0,
  fontSize: '14px',
});
